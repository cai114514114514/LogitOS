# 官网交付检查

## 内容核对

阅读根目录 AGENTS.md、CLAUDE.md 的子系统说明与带日期修正、README.md、
TRANSPARENCY.md，并核对下列当前维护文档和对应源码入口：

- AetherScript 维护图、3.x 语言说明与迁移记录；示例取自 hello.as。
- Code Studio 引擎维护说明。
- OpenLogit SDK、2D/3D 目录说明、Scene Studio 说明，以及现存 Island 来宾结果。
- Project 工作空间、TextEdit、默认模型任务验收记录。
- LogitFS 日志源码、文字绘制入口、IME 切换常量、字体和音频驱动验收记录。

官网把历史验收与当前源码描述分别陈述。未重新运行操作系统测试；没有将
AetherScript 子集写成完整自举，将软件 3D 写成 GPU 兼容，或将 Project 专用
磁盘的持久身份写成默认启动已完成迁移。

## 构建与静态检查

- `npm run check`：通过。
- `npm run build`：通过；产物只包含官网公开 HTML、CSS、JS 和图片。
- Prettier 检查：通过。
- frontend-design-premium 严格静态审查：0 errors、0 warnings。
- 所有本地页面、样式、脚本和引用图片均返回 HTTP 200。
- 开发服务与静态产物首页逐字节一致；无未展开的 include 标记。
- 五份系统截图与记录的原图逐字节一致。
- GitHub 仓库、贡献、透明度、许可等外部入口已检查；未公开的本地报告使用
  官网验证说明页承接，避免失效的 GitHub 链接。

## 前一轮浏览器交互基线

使用独立 Playwright 会话检查构建产物：

- Finder、Browser、TextEdit 切换实际图片；方向键与 Home 更新选中状态。
- 图片弹层打开、Escape 关闭并回到触发按钮；底层页面无法获得焦点。
  原生 dialog 的 Tab 可到浏览器界面，这是浏览器行为，不伪称自定义焦点循环。
- 复制真实写入剪贴板；拒绝复制时选中命令并显示手动复制提示。
- FAQ 键盘展开、手机菜单打开、导航后收起。
- 1440、1024、768、600、390、320 像素宽度下无页面横向溢出。
- 减少动态效果偏好禁用平滑滚动。
- 禁用 JavaScript 时新增正文完整，FAQ 可用，依赖 JS 的控件保持禁用。
- 验证说明页的图形、语言、系统能力锚点存在；手机布局无溢出。
- 浏览器控制台最终 0 errors、0 warnings。

截图与检查记录保存在本地忽略目录 `output/`，不进入静态站点产物。
这些检查针对宿主浏览器，不代表 LogitOS 内置浏览器已经兼容官网。

## 交付边界

### 滚动场景重做验证

独立 Playwright 会话针对当前开发服务执行 28 项检查，全部通过，记录在
`output/story-checks.log`，脚本为 `output/story-check.cjs`。本轮覆盖：

- 桌面展开、任务侧栏特写与架构分层随实际滚动位置变化；舞台保持固定。
- 反向滚动回到同一位置时，桌面变换完全一致。
- 键盘阶段按钮选择任务；架构按钮展开并选择对应说明；普通锚点离开展示。
- 1440 × 1000 的任务特写完整容纳真实截图，侧栏没有被视口裁掉。
- 1024、900、768、390、320 像素宽度无页面横向溢出；人工查看窄桌面的
  任务特写、架构分层和手机首屏，修正了架构图的溢出及说明重叠。
- 小屏、减少动态效果和禁用 JavaScript 时，三段桌面说明及四段架构说明均可读。
- 运行中切换减少动态效果取消动画并移除脚本变换，恢复静态页面高度。
- 应用切图、弹层 Escape 及焦点恢复、独立验证说明页继续可用；0 页面脚本错误。

旧 `output/motion-checks.log` 针对已移除的轻量首屏，不能作为当前场景验证。

对 requestAnimationFrame 请求计数的 1 秒空闲观察未出现新增请求，证明当前
动画逻辑没有持续 JS 帧循环；这不是整机 GPU 功耗测量。语法检查、严格静态审查
和官网构建再次通过。关键帧截图保存在 `output/playwright/story-*.png`。

本地预览已完成，尚未公开托管。素材发布前事项见 assets/README.md。
未修改操作系统源码或构建入口，未控制现有 QEMU 或改写其磁盘。
