{
  "name": "logitos-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "check": "node --check app.js && node --check tour.js && node --check motion.js && node --check scenes.js && node --check scripts/serve.mjs && node --check scripts/build.mjs && node --check scripts/render.mjs"
  }
}
