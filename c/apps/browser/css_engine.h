#ifndef LOGIT_CSS_ENGINE_H
#define LOGIT_CSS_ENGINE_H

/* M17 L2: LibCSS-backed implementation of the css.h interface (css_init /
 * css_apply). Drop-in replacement for net/css.c -- same struct cstyle output,
 * so net/layout.c and the browser need no changes. */

#endif /* LOGIT_CSS_ENGINE_H */
