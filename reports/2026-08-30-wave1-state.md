# Wave-1 orchestration state — browser engine completion, 2026-08-30

Written by the orchestrator at 13:30, minutes after all eight remaining wave-1
agents died on the account's **[1308] 5-hour usage cap** (reset 16:31:20).
This file is the resume point: it records what each package is, who owns what,
what the dead agents left uncommitted, and how to re-dispatch. If this file and
the git log disagree, the git log wins — re-run `git log --oneline` and
`git status` and annotate below.

## Where things stand

- Baseline: `9e96e8035` (the prior browser roadmap arc, 158 files) +
  `1461cd173` (nine pre-wired empty gate fragments) + `6bf6ff4fb` / `7bad25f12`
  (**bold package — the only one DONE**: gates with watched-red negctls and
  scanout numbers; CLAUDE.md corrected beside the old claim).
- Dead mid-flight at 13:24, all uncommitted, all inside their owned files:
  tls, crash-cluster, docwrite, video, anim, storage-kernel, testdebt, canvas.
  frameworks never started (died on [1302] at 12:49; canvas died the same way
  once and its re-dispatch died on [1308]).
- A SEPARATE live agent works the ssh/terminal arc — its files are
  never-touch for us: `c/net/ssh/**`, `c/apps/coreutils/sshd.c`,
  `c/apps/as/as_native.c`, `fsroot/as/lib/abi.as`, `c/kernel/sched/kbench.h`,
  `tests/ssh_attack.mk`, `tests/boot/*ssh*`, `tests/boot/run-launch-profile.sh`,
  `tests/qmp/{qmp_ui,qmp_rich_term,qmp_lm_stream,qmp_damage,qmp_cjk_term,qmp_audio_term,qmp_motion,qmp_launch_click}.py`.
  It shares the same account quota; it is probably also dead until 16:31.
- `make test-mk-wired` is green except the pre-existing unreachable
  `tests/ssh_attack.mk` (the ssh agent's own, untouched by rule).

## Resume protocol (after quota reset)

**UPDATE (orchestrator, post-mortem survey): `make BUILD=build-bridge
build-bridge/disk.img` exits 0 WITH all eight dead agents' uncommitted edits
in place — the tree needs no repair; each re-dispatched agent audits and
continues its own files' half-work. Quota confirmed reset by the owner;
resume began at max-3 concurrency, later raised to 8 at the owner's call.**

**UPDATE 2 (the exit-0 above was PARTLY VACUOUS — two landmines found since,
both fixed by orchestrator arbitration):** (a) the storage agent's
SYS_FTRUNCATE was spelled in as_native.c/abi but not in complete.c's mirror
— check-asops reddened every FRESH tree (`f23f94507` fixed; cached trees hid
it); (b) the canvas agent's text trio used CV_THIS before its #define — a
fresh compile of js_canvas.c died (`2a35e17dd` fixed; the "exit 0" above
never compiled it because build-bridge's .o was 12:39-cached, older than the
13:24 edit). LESSON for the record: a warm-tree exit-0 proves nothing about
files edited after the tree's objects; the honest check is a FRESH tree
(`make BUILD=build-smoke build-smoke/disk.img`, running as of 13:5x) or
rm'ing the specific object and rebuilding it.**

**UPDATE 3 (smoke result, 13:48 — and the smoke command itself was flawed):**
the smoke build FAILED (css_extra.o: 20 errors, all `g_src` undeclared at
:1376-1397 — the dead anim agent used the variable in its @keyframes capture
and documented it in comments at :1280-1282 + css.h:364 but never wrote the
declaration), and the failure was initially missed because the orchestrator's
command was `make ... | tail` — the pipe masked make's exit code with tail's
0. TWO lessons now on record: warm-object exit-0 proves nothing about files
edited after the objects, and `| tail` proves nothing at all — use
`set -o pipefail` or read \${pipestatus}. The css_extra.c fix belongs to the
LIVE anim agent (owner, running); it was messaged to make-it-compile first
as its own commit because video/crash/docwrite's browser.elf is blocked on
it. The rest of the smoke tree was still building when css_extra.o died —
more landmines may exist beyond it; re-run the smoke build after anim lands.

1. `git log --oneline -8` + `git status --short` — see what changed since
   `7bad25f12` (the dead agents' uncommitted edits are expected to still be
   there, unless their re-dispatch already committed).
2. Verify the tree builds: `make BUILD=build-bridge build-bridge/disk.img`.
   If it fails, the most likely blocker is a half-edit in a dead agent's own
   file (anim's `css_interp.c` was mid-edit when bold's agent saw it break at
   12:50). Repair belongs to that package's re-dispatched agent, not to you.
3. Re-dispatch the unfinished packages as background general-purpose agents,
   **max 3 concurrent** (10-wide hit [1302] rate-limit; the burst helped burn
   the 5-hour cap). Launch a new one only when one completes. Order (site
   impact first): tls, crash-cluster, docwrite, video, anim, canvas,
   frameworks, storage-kernel, testdebt.
4. Each agent brief = the COMMON block below + the package's OWN/MISSION.
   The briefs are deliberately self-contained; paste them verbatim.
5. After all nine land: the wave-end backlog (below), then the orchestrator
   review (build + `make test-mk-wired` + diff read + scoreboard rerun), then
   wave 2 planning (multi-document browsing contexts first).

## COMMON block (paste into every brief)

Workspace /Users/wangzhe/system/LogitOS, from-scratch x86 OS + browser, host
macOS arm64, several sibling agents in parallel. Read AGENTS.md IN FULL first
(binding). Build ONLY with `make BUILD=build-<pkg> build-<pkg>/disk.img`
(never the default build/ tree; NEVER `make clean-scratch`). Commits
path-limited only (`git add <paths>`; never -A/-u/.; retry index.lock after
5s; never push). Your `-include tests/<pkg>.mk` line already exists in the
Makefile — never edit the Makefile. `make test-mk-wired` must stay green
except the pre-existing unreachable `tests/ssh_attack.mk` (never touch).
NEVER TOUCH: the ssh/terminal arc files listed above, `tests/scoreboard/**`,
`.zcode/`, `sshatk-build/`, AGENTS.md, CLAUDE.md (corrections go in your
report), Makefile, third_party/** unless granted, and every file not in your
own-list (needed fixes → exact diff in your report). Before editing any file
you own, `ls -la` it; mtime <10 min → wait and re-check once, then skip and
report. Negative controls WATCHED RED first, output recorded. Measure in the
guest (QEMU) for browser/OS behavior; host wall-clock is worthless. Absent
beats present-and-wrong. Comments = the deliverable (why-not-alternative,
measured numbers + method, traps, deliberate non-features). macOS: `stat -f
%z`; bash 3.2 (empty array + set -u fatal); memset/memcpy are macros after
<_string.h> (guard #ifndef); weak syms via include/weaksym.h +
LOGIT_WEAK_STUB in the same commit. QEMU boots 1 GiB each — checkpoints only.
If a tool call errors [1302]/[1308], wait 60s (or, for 1308, STOP and report
— the 5-hour cap cannot be waited out inside a run). A previous agent died
mid-file in your package (see per-package note): run the baseline gate FIRST;
if your owned file is half-edited, complete or minimally revert the half-edit
with a comment — it is yours.

REPORT: (1) what you did, before/after guest numbers; (2) gates + ci wiring +
what each printed while RED; (3) deliberately-not-done + why; (4)
unfinished/failed honestly; (5) exact diffs for files you don't own.

## Packages

**DONE: crash-cluster (commit `3b22e3b68`, ~14:0x).** Fixed: dynamic
data-block scripts executed (offer_scripts had no type whitelist — the parse
path always did); HTMLScriptElement.text landed as an expando instead of
child text (the loader idiom createElement+text+append silently ran nothing);
stripe's ld+json/__NEXT_DATA__ blocks eval'd as JS. Guest: fixture 2→0
exceptions, stripe 2→0, google-search 2→0 (postMessage overload fix was
already in the arc), douyin verified via currentScript marker. Gates:
test-crashfix chain with caller-ratchet → negctl → positive, watched red,
mk-wired green. kimi Function.prototype.caller deliberately NOT done
(quickjs unowned): ratcheted via tests/fixtures/crashfix/CALLER-BASELINE +
exact patch in its report — see the wave-end backlog.

**DONE (partial, in flight): anim's css_extra.c compile fix (`a73a969b1`)** —
three use-before-declaration sites (g_src AND kmalloc/printf), verified by
forced fresh compile + browser.elf link + disk exit 0. Its host checker
(check_anim_clock.c, newly written) caught 5 real bugs in the recovered
engine (ease-in-out parse, bezier 1/256 accuracy, steps() formulas, parse
clobbering, iteration-count), 80/80 green; guest gate running. Also:
mk-parse is now wave-wide CLEAN (`make -n BUILD=x x/disk.img` exit 0) after
the fourth landmine (tests/cssweb.mk:86 lost '#' on a moved comment — same
testdebt mistake-pattern as webapi_globals.mk:77; both resolved).

**DONE: docwrite (commit `48a2ebd96`, ~14:2x).** document.write/writeln
per spec: mid-parse insertion at the insertion point, ordered, nested
writes execute in place (a WRITTEN script ran and its own w4 write landed);
after-load write IGNORED with zero pixel change (447==447). THREE watched
reds recorded verbatim in tests/docwrite.mk (pre-feature build, revival
falsifier, after-load falsifier). Cross-package defect found and fixed:
crash-cluster's el_set_html stamping (13:22) killed written scripts —
revival rebuilds them fresh in the detached scratch before insertion
(main-parser semantics for document.write; innerHTML semantics preserved).
NOTE: its negctl could only run via manual recipe lines at the time
(check-asops was still red); CLOSED by orchestrator at ~14:3x —
`make BUILD=build-docwrite test-docwrite` end-to-end exit 0 with both
sides' PASS lines in the log (negctl disk-ndw.img re-walked the .la graph
through the now-green check-asops). WARNING it left: today
carried LYING-TIMESTAMP stale .o files (e.g. a 13:24 css_interp.o with
115 symbols predating the 13:08 source) — anyone hitting weird
link/compile results should rm today's stale .o files first.

**DONE: storage-kernel (commit `a2aebbff7`, ~14:1x).** SYS_FTRUNCATE 189
completed (the dead agent was ~90% done; audited + gates filled). Real info
leak fixed en route: ftruncate-grow holes persisted 10/16 uninitialized
kernel-heap bytes into ring-3-readable files (now zero-filled). Gates:
test-storage 29/29 host (real file.c vs contract model, ASan+UBSan);
test-storage-negctl watched red with exactly the 8 assertions the fix owns;
test-storage-os = 2 boots + persistent disk + byte-exact reboot re-read;
ci-host/ci-boot wired. Deliberately absent: pwrite, rename-replace (logitfs
rename is no-clobber by the crash line's design), qmp_storage.py (a stub
would be worse than none). WAVE-2 SIGNATURE (browser-side storage, from its
report): whole-store save = OPEN(O_WRONLY|O_CREAT|O_TRUNC) → WRITE → FSYNC →
CLOSE (shrink included, no ftruncate needed); partial in-place = O_RDWR →
LSEEK → WRITE (byte-exact); length-only = SYS_FTRUNCATE(fd,len) (grow
zero-fills, shrink cuts, cursor unmoved); path-visible only after FSYNC or
last CLOSE; FTRUNCATE -1 is ONE bit {bad fd, wrong file kind, len<0,
alloc-fail} — do not branch on which; do NOT design on rename-over
(refused). Cross-file diffs pending in the backlog (libc io.c ftruncate
body; js_idb.c:33 + js_cache.c:58 stale-both-ways comments).

**DONE: tls (commit `0b0b610ab`, 13:41).** Root cause: jd CDNs send a
duplicated GlobalSign intermediate; the old strict-order chain check refused
it. Fix: path-building verification (parent search over the whole flight,
signature-verified, dedup + in-place reorder, pathlen out; OCSP staple now
reads the leaf's real issuer). Gates: test-tls-chain 10/10 offline with
watched-red + 2-control negctl; test-tls-chain-live 6/6 on the real wire
(incl. cloudflare X25519MLKEM768 hybrid — now gated — and both previously
refused 360buyimg CDNs); fresh ISO exit 0. NOT done: the in-guest bing
scoreboard rerun (was blocked by check-asops at the time) — added to the
wave-end backlog as the live differential proof. The check-asops blocker
itself was fixed by the orchestrator at `f23f94507` (SYS_FTRUNCATE mirror
half in c/apps/as/complete.c).

### tls — bing/jd TLS handshake refused (FETCH-FAIL while host gets 200)
OWN: c/net/tls/**, tests/tlsfix.mk, tests/fixtures/tls/**. Refs (read-only):
c/net/http/**, tests/qmp/qmp_browser_https.py, qmp_site.py.
MISSION: capture real chains host-side (`openssl s_client -showcerts`), drive
OUR verifier over them in a host unit harness (source lists = subtractions
from Makefile vars, never hand-copied). Check in order: chain building (leaf
only, no AIA), signature algorithms (RSA-PSS/curves), validity/extensions,
X25519MLKEM768 interop + fallback, ALPN. Fix GENERALLY, no hostname branches.
Gate: test-tls-chain accepts bing+jd chains, rejects a corrupted one (negctl,
watched red pre-fix). If the host has outbound net, one-site scoreboard rerun
for bing as the live proof; else say so honestly.
LEFT BY DEAD AGENT: edits in tls.c, tls12.c, tls_int.h, x509.c, x509.h
(13:24). Audit what they do before continuing — the diagnosis may be done.

### crash-cluster — four specimen JS crashes
OWN: js_webapi.c/h, js_select.c, js_dom.c/h, tests/crashfix.mk,
tests/fixtures/crashfix/**, tests/qmp/qmp_crashfix_page.py.
MISSION: (a) google-search "postMessage target-origin SyntaxError" (chase the
FIRST exception; our postMessage targetOrigin validation vs HTML spec; the
strict URL parser may reject an origin form browsers accept); (b) kimi fm.js
"caller of undefined" (Function.prototype.caller — QuickJS returns undefined
where browsers return the function; fix belongs in third_party/quickjs or
js_platform.c → REPORT the diff, don't edit); (c) douyin "getAttribute of
null" (a querySelector a real browser satisfies returned null — selector gap
in js_select.c); (d) stripe 2 syntax errors (feed specimens to our QuickJS
parser host-side, find the construct). Tool: tests/qmp/qmp_replay.py replays
saved specimens in the guest. Fix only general defects in YOUR files.
LEFT BY DEAD AGENT: edits in js_dom.c, js_dom_iface.inc (13:22), tests/
crashfix.mk (13:23).

### frameworks — 5/7 framework apps must mount
OWN: js_semantics.c, js_module.c, js_dom_iface.inc (SHARED with crash-cluster
— coordinate: crash-cluster edits interfaces, you edit semantics/modules;
if you both need js_dom_iface.inc, the later one rebases and reports),
tests/fixtures/frameworks/**, tests/fragmk.mk.
MISSION: only react+vite mount today. Find the corpus driver
(`grep -rn frameworks tests/*.mk tests/qmp/`), re-measure today's first
exception per app (do not trust BASELINE), fix general DOM/document/module
gaps. Update BASELINE with justification. No framework-name branches ever.

### docwrite — document.write/writeln
OWN: js_platform.c/h, html_tree.c, html_tokenizer.c, tests/docwrite.mk,
tests/fixtures/docwrite/**, tests/qmp/qmp_docwrite.py.
MISSION: implement per HTML spec: mid-parse (script nesting >0) → insert
markup at the insertion point, ordered; after load → IGNORE (spec: do
nothing; never wipe). The old "zero corpus hits" refusal comment in
js_platform.c gets the OLD CLAIM KEPT BESIDE THE CORRECTION (python.org and
2345 measured needing it). Check js_platform.c's header wrap-list rule —
same-commit compliance. Gate: inline-script write → pixels + DOM order;
after-load write must NOT change pixels (negctl: pre-fix build reddens the
mid-parse assertion).
LEFT BY DEAD AGENT: js_platform.c (13:14), tests/docwrite.mk (13:06).

### video — player stalls + <video src> + subs + h265-b
OWN: js_media.c/h, js_media_src.c, c/lib/media/**, c/lib/video/**,
c/apps/media/demuxcheck.c, tests/mse.mk (extend), tests/videosrc.mk,
tests/fixtures/{video,mse}/**.
MISSION: (1) test-mse-os RED: stalls "decoded=60 shown=59", audio master
clock (sound-card play cursor, js_media.h:79-87) stops — run it red first,
diagnose, fix, green. (2) <video src> direct playback does not exist: src →
bfetch (browser_rt.c is READ-ONLY: report diffs) → demux → decode →
avclock+snd_* → blit on the 30Hz chain. (3) subs.c WebVTT parsed but
unreachable — render <track> cues as pixels. (4) test-h265-b 79/80: find the
failing case's diff; fix or document precisely. Gates count FRAMES SHOWN and
captured PCM, not changed pixels; mse negctls stay prerequisites.

### anim — CSS animation/transition clock
OWN: css_interp.c/h, css_extra.c, css.h, js_page.c/h, js_anim.c, browser.c/h,
tests/anim.mk, tests/fixtures/anim/**, tests/qmp/qmp_anim_page.py.
MISSION: today animation/transition collapse to end-state (css_extra.c
header + ~:1108). Build: tick on the page deadline queue/rAF in js_page.c
(no second clock); capture @keyframes + animation/transition declarations
(css_report.c is read-only — report gaps); interpolate via css_interp;
overlay like js_anim.c's WAAPI does; scoped relayout/repaint via browser.c.
Cap concurrent animations, document cap + degradation. Gate: @keyframes
opacity 0→1 + translateX over 2s; screenshot t≈1s; mid-frame ≠ start AND ≠
end. Negctl: pre-fix (end-state) reddens mid≠end — watch it.
LEFT BY DEAD AGENT: extensive edits (css_interp.c/h 13:08, css_extra.c
13:12, css.h 13:22, js_anim.c 13:22, js_page.c 13:23, browser.c 13:24) —
the boldest half-edit of the eight; bold's agent saw css_interp.c reference
CI_EASE_CUBIC/struct ci_ease before definition at 12:50. AUDIT FIRST: the
dead agent may have been close to done.

### canvas — fillText/strokeText/measureText/drawImage
OWN: js_canvas.c, tests/canvas.mk (extend; existing gates intact),
tests/qmp/qmp_canvas_readback.py (exists untracked from an older arc — read
first), tests/fixtures/canvas/**.
MISSION: measureText (shaping-aware width); fillText/strokeText (shape via
c/lib/text read-only, glyphs → gfx_path per glyphras.c, honor styles +
transform; maxWidth squash documented); drawImage 3 arities from
HTMLImageElement + canvas (nearest-neighbor OK if commented). TRAPS: surfaces
are RGBA8-aliased; globalCompositeOperation present-and-wrong DOCUMENTED
(:180-196) — leave documented; toDataURL(PNG) must keep passing. Negctl:
pre-fix build (fillText contributes nothing) reddens the gate.
LEFT BY DEAD AGENT: js_canvas.c (13:24).

### storage-kernel — the durable-storage primitive, kernel side only
OWN: kernel fs/exec files it discovered (c/kernel/exec/** per the dead
agent's trail), syscall table + include/abi/ headers for its calls,
tests/storage.mk, tests/unit/ (new), tests/qmp/qmp_storage.py.
MISSION: step 0 MEASURE first (what can ring-3 do today: O_TRUNC rewrite?
append? mid-file pwrite? grow? shrink? rename-over?) with a guest probe —
the js_idb.c:33-38 "no VFS positional write" comment may be stale. Then the
minimal honest primitive (pwrite/ftruncate or atomic temp+rename — whole-file
rewrite is what localStorage/IDB/cookies need). Real success/errors only
(ENOSYS-beats-lies). Gates: host unit + guest write→rewrite→grow→shrink→
re-read; negctl = ENOSYL/stub red. Signature sketch for wave-2 browser side
in the report.
LEFT BY DEAD AGENT: c/kernel/exec/file.c/h, c/kernel/exec/syscall.c,
include/abi/logit_abi.h, logit_calls.abi (13:2x) — a syscall was being born;
audit the ABI numbering before continuing.

### testdebt — make the apparatus honest
OWN: tests/qmp/*.py (except never-touch list; qmp_ui.py additive-only,
mtime>30min), tests/*.mk EXCEPT tests/mse.mk + tests/canvas.mk (siblings),
tests/audit-unwired.baseline (may only shrink), tests/audit-stranded.baseline,
tools/check-test-liveness.py.
MISSION: (1) liveness RED on 7 scripts — five drivers click the address bar
inside the WM TITLEBAR and pass on start-focus luck (CLAUDE.md ~L1538): fix
drivers to real geometry (browser.c:113-159) or assert focus first; never
weaken assertions; genuine app bugs stay red + reported. (2) Wire
test-live-page, test-dom-bindings, test-webapi-page, test-forms, test-tabs,
test-css-fidelity, test-css-modern-os into ci inside their OWN fragments;
cannot-run-on-host ⇒ SKIP LOUDLY. mse/canvas wiring lines go in the REPORT
(orchestrator applies). (3) Stranded negctls → prerequisites of positives in
same fragment (browser ones at least). Every wired gate must actually RUN
once in-session with one output line recorded.
LEFT BY DEAD AGENT: audit-stranded/audit-unwired baselines + reftest.mk,
wpt.mk, clip.mk, motion.mk edits (13:03) — likely partial wiring.

## Wave-end backlog (orchestrator applies after all packages land)

1. css_engine.c:1166-1169: `>= 700` → `>= 600` for the bold collapse
   (CSS font matching; bold's gate page uses 700 and stays valid).
2. `text_line_height_face(int face, int px)` in c/kernel/gui/text.{h,c} +
   tests/unit/font_weight_test.c using it — land both halves together
   (bold agent's report §8 has the exact diff).
3. Re-run `make BUILD=build-bold test-bold-page` end-to-end once anim's
   css_interp.c lands (blocked at 12:50 by the mid-edit).
4. Apply testdebt's reported mse.mk/canvas.mk ci-wiring lines.
5. **libc ftruncate rewire (storage-kernel's report):** c/apps/libc/src/io.c
   ~:130, replace ftruncate() body with `long r = sys(SYS_FTRUNCATE, fd,
   len, 0); if (r < 0) { errno = (sys(SYS_LSEEK, fd, 0, SEEK_CUR) < 0) ?
   EBADF : EINVAL; return -1; } return 0;` — with a comment: SYS_FTRUNCATE
   since the storage wave 2026-08-30; kernel -1 is one bit (contract in
   logit_abi.h) so errno is most-probable-shape not promise. truncate()
   wrapper unchanged.
6. **js_idb.c:33 + js_cache.c:58 comment corrections (storage-kernel's
   report):** keep the old sentence beside its correction — it was stale
   BOTH ways (positional write DID exist as lseek+write byte-exact; the
   cited "CLAUDE.md structural gap #3" does not exist — the citation
   dangles). The missing piece was only a length change, now SYS_FTRUNCATE;
   the session-scoped refusal stands on scope alone; backing wiring is the
   wave-2 browser-side line's work.
7. **quickjs Function.prototype.caller patch (from crash-cluster's report,
   exact instructions):** in `js_function_proto_caller`
   (third_party/quickjs/quickjs.c:~14993, after the strict/has_prototype
   TypeError check), walk `for (sf = ctx->rt->current_stack_frame; sf;
   sf = sf->prev_frame)` matching `JS_VALUE_GET_TAG(sf->cur_func) ==
   JS_TAG_OBJECT && JS_VALUE_GET_OBJ(sf->cur_func) ==
   JS_VALUE_GET_OBJ(this_val)` (the identity rule js_build_backtrace uses at
   :6753-6766); on match return `sf->prev_frame`'s cur_func dup'd, JS_NULL
   when NULL/detached. TRAP: the same C function is installed as the getter
   for BOTH `caller` AND `arguments` (:54075-54083) — the patch must split
   the installation or `.arguments` starts returning a function. Land with
   tests/fixtures/crashfix/CALLER-BASELINE flipped (the ratchet crash-cluster
   left red-by-design; it goes green when the gap closes).
6. Re-run full scoreboard vs the 4/21 PAINTED baseline; then wave 2
   (B1 multi-document browsing contexts → B2 cross-origin iframe +
   Cloudflare specimen → B3 popups → B4 background tabs; then WebAudio,
   WebGL, subtle-crypto, VP9, ServiceWorker execution, browser-side storage,
   HTTP cache, Intl/IDNA — all at ≤3 concurrency).
