# 把 LogitOS 浏览器补成"真正的浏览器引擎"——两波子代理施工计划（Flash 档执行版）

## 目标与执行方式
- **目标**：现有 97k 行引擎（WHATWG HTML + LibCSS + flex/grid + QuickJS + TLS1.3/HTTP2 + MSE/wasm）基础上，把记分牌从 4/21 PAINTED 拉起来，补齐浏览器上下文与全部点名能力，成为完整引擎。
- **额度策略**：子代理尽量走 GLM-5.3-Flash（省额度）。我的 Agent 工具没有模型参数，实际路由由客户端设置决定——若你把子代理模型切到 Flash 即生效。为适配较弱模型：每份任务书写死精确文件清单、构建命令、验收 gate、禁止事项与"做不到就留空+注释"的硬规则；**每波结束由我亲自复核构建/gate/diff**，质量关不外包。

## 第 0 步：接管基线（我亲自做）
1. 上一个 agent 的未提交工作（22 文件 +3919 行）按路径限定提交为基线 commit。
2. `make build/disk.img BUILD=build-bridge` 验证可构建 + `make test-mk-wired` 绿；构建不过先修构建。

## 缺口清单（全部纳入，范围不收窄）

### A. 站点打不开的直接原因（第一波）
- A1 TLS 握手被拒（bing-search/jd FETCH-FAIL，host 200 guest 拒绝）→ 诊断证书链/算法/扩展/ML-KEM 互操作，修 c/net/tls。
- A2 站点 JS 崩溃簇：kimi(fm.js "caller of undefined")、google-search(postMessage targetOrigin/solveSimpleChallenge)、douyin(getAttribute null)、stripe(2 语法错+GAP5)、bing 搜索结果页标本——qmp_replay.py 逐个诊断，修通用缺陷，禁止按站点分支。
- A3 框架不挂载：frameworks 语料 7 分之 5 白屏（svelte/vue/webpack/angular/next）。
- A4 document.write/writeln（python.org/2345 需要；旧"零命中"结论已被记分牌推翻）。
- A5 视频停摆：test-mse-os 红（decoded=60 shown=59、audio clock 不前进）；<video src> 直连播放不存在；subs.c 解析了但不可达。
- A6 粗体不渲染：ui-bold.ttf 已在盘上，缺"文本 ABI 粗细参数→内核 face 选择→painter 读 o->bold"三步接线；斜体维持拒绝（无 ital 轴、拒绝剪切）并把 CLAUDE.md 新旧说法并列修正。

### B. 浏览器上下文（第二波基石）
- B1 多文档 browsing context：拆 layout/css_engine/js_page/dom 单例 → 每文档状态 + frame 树 + 嵌套布局/绘制。
- B2 跨源 iframe：frame <script src>、window.parent/postMessage、独立 JS 上下文 → Cloudflare turnstile 作验收标本。
- B3 window.open/弹窗/target=_blank（现返回 null）；B4 后台标签页 JS。

### C. 保真度
C1 Canvas drawImage/fillText/strokeText/measureText；C2 CSS animation/transition 真时钟（现只取终态）；C3 @font-face 网络字体（document.fonts 现在立即 resolve）；C4 存储持久化（先补内核 VFS 位置写/原子替换，再落 localStorage/IDB/cookies）；C5 HTTP 磁盘缓存。

### D. 大型 API（全部）
D1 WebAudio；D2 WebGL（软光栅 WebGL1 子集起步）；D3 ServiceWorker 执行（js_swreg.c 写明的两个阻塞点）；D4 WebRTC（DataChannel over DTLS 起步）；D5 crypto.subtle 真操作；D6 Intl/Segmenter + 完整 UTS-46 IDNA；D7 VP9 解码器（让已提交的 gate 变诚实）。

### E. 测试与卫生（贯穿）
E1 check-test-liveness 7 红（地址栏五驱动撒谎）；E2 浏览器关键 gate 接进 ci-boot/ci-host（host 不支持的 LOUD SKIP）；E3 54 个搁浅负控改为前置；E4 test-h265-b(79/80)、test-vidbench-guest 清账；E5 jsfb NARROW 基线；E6 reftest 级像素正确性盲区。

## 第一波：10 个后台代理并行（文件所有权互斥）
| 包 | 独占文件 | 验收 |
|---|---|---|
| tls | c/net/tls/**, 新 tests mk | 本地 TLS server 回放握手成功 + 记分牌 FETCH-FAIL 消失 |
| crash-cluster | js_webapi.c, js_select.c, js_dom.c | 每根因一个标本回放 gate + 负控先看红 |
| frameworks | js_semantics.c, js_module.c, js_dom_iface.inc | BASELINE 更新，≥5/7 mount |
| docwrite | js_platform.c, html_tree.c | fixtures 回放 gate + 负控 |
| video | js_media*.c/h, c/lib/media/**, c/lib/video/** | test-mse-os 绿 + 帧/PCM 计测 + 字幕像素 gate |
| bold | c/kernel/gui/text.c, browser_paint.c, c/lib/text | reftest 像素可辨粗体 |
| canvas-text | js_canvas.c | canvas 扩展 gate + 负控 |
| anim-clock | css_interp.c, css_extra.c, browser.c | 动画中间帧随时间变化的像素 gate |
| storage-kernel | 内核 fs 位置写/原子替换 | 内核单测 + 负控 |
| testdebt | tests/qmp 地址栏驱动, tests/*.mk | liveness 绿 + mk-wired 绿 |

每份任务书硬性条款：BUILD=build-<包名>；路径限定提交（禁止 git add -A）；负控先看红再修；guest 内计测、不许 host 墙钟；注释按 AGENTS.md 标准；做不到就留空+注释并如实报告；不按站点分支、不删 gate、不改所有权外的文件。

## 第二波（第一波落地、我复核后派）
B1 多文档架构（最大包，串行分三段：单例拆分→frame 树嵌套布局→跨源隔离+Cloudflare 验收）→ B2/B3/B4 跟随；D1 WebAudio、D2 WebGL、D5 subtle、D7 VP9 并行（多为新文件）；D3 ServiceWorker、C4 browser 侧存储、C5 HTTP 缓存、D6 Intl/IDNA 随后；第一波未修完的根因继续。最后全量记分牌对比 4/21 基线 + Cloudflare 页可交互。

## 完成定义（每包）
disk.img 构建过；gate 已接线且负控是前置并看过红；guest 计测有前后数字；注释达标；路径外零改动；报告如实写"做了/故意没做/没做完"。